# FastAPI base URL the React app talks to. Local dev default matches
# `uvicorn api.main:app --reload`'s default port; set this to the deployed
# Render URL (e.g. https://your-api.onrender.com) in production.
VITE_API_URL=http://localhost:8000
